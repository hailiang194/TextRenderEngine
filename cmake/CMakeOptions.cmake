# Enable to build Text Render Engine's examples
set(BUILD_TEXT_RENDER_ENGINE_EXAMPLES ON CACHE BOOL "Enable to build TextRenderEngine examples")

# Enable to build the engine as shared lib
set(BUILD_SHARED_LIBS ON CACHE BOOL "Enable to build the engine as shared lib")